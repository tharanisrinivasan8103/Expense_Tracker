// Transaction model removed — kept as a harmless placeholder to avoid breakage
// The project now uses `Income` and `Expense` models instead of a combined Transaction model.

// If you want to completely remove this file, delete it. Keeping this stub prevents import errors
// in older branches during a gradual cleanup.

export default null;
